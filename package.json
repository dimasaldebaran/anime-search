{
  "scripts": {
    "dev": "vite",
    "build": "vite build",              // <— change to this
    "typecheck": "tsc -b --pretty false",
    "preview": "vite preview --port 4000"
  },
  "engines": { "node": "20.x" }         // optional but recommended
}
